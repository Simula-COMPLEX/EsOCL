package tudresden::ocl20::pivot::examples::royalsandloyals

-- Invariant 1:
context Customer

inv ofAge: self.cards->size() = 1 and self.isMale = false

-- inv ofAge: not (self.isMale = false)

-- inv ofAge: self.cards->size() = 1 and self.isMale = false

endpackage