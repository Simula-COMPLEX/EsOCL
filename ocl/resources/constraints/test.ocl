package tudresden::ocl20::pivot::examples::royalsandloyals

-- Invariant 1:
context Customer
inv name: self.name = 'BB'
inv ofAge: age >= 18
inv size: cards->size() > 3
inv cards:  cards->forAll(printedName='AA')
inv dd: cards->select(printedName='AA')->size()<=2

-- Invariant 2:
context CustomerCard
--inv checkDates: validFrom.isBefore(validThru)
def: a: String = self.printedName
def: b: Boolean = valid
inv c: self.validFrom = null
inv d: self.owner.name = 'BB'
inv name: self.printedName = 'AA'
inv boolean: self.valid = true

endpackage